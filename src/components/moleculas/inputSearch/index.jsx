const inputSearch = () => {
  return (
    <section>
      <input
        type="search"
        placeholder="Search Pokemon, Move, Ability etc"
      ></input>
    </section>
  );
};

export default inputSearch;
